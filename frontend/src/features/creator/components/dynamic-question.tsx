"use client";

/**
 * Renderiza la pregunta actual del árbol de decisiones.
 * Soporta: text, textarea, boolean, select, multiselect, catalog-select, catalog-multiselect.
 */
export function DynamicQuestion() {
  return <div>DynamicQuestion — por implementar</div>;
}
