"use client";

/**
 * Pantalla de bundle completado: descarga JSON/ZIP, preview de artefactos, guía de instalación.
 */
export function CompletionScreen() {
  return <div>CompletionScreen — por implementar</div>;
}
