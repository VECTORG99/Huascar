/**
 * Gestiona persistencia en sessionStorage: answers, cursor, navigation phase.
 */
export function useCreatorPersistence() {
  // TODO: Extraer lógica de sessionStorage del StepProvider original
  return {};
}
